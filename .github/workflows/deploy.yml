name: Atualização, Compilação Rust e Deploy

on:
  schedule:
    # Executa todos os dias às 08:00 UTC (05:00 da manhã no horário de Brasília)
    - cron: '0 8 * * *'
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Rust Toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable

      - name: Cache Cargo Dependencies
        uses: swatinem/rust-cache@v2

      - name: Build Rust Server
        run: cargo build --release --manifest-path rust-server/Cargo.toml

      - name: Build Rust Scraper (Crawler)
        run: cargo build --release --manifest-path rust-scraper/Cargo.toml

      - name: Copy Compiled Executables to Bin
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path bin
          Copy-Item -Path target/release/rust-server.exe -Destination bin/server.exe -Force
          Copy-Item -Path target/release/rust-scraper.exe -Destination bin/crawler.exe -Force

      - name: Run Compiled Rust Crawler
        run: bin/crawler.exe

      - name: Commit and Push Updates
        shell: pwsh
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          
          # Verifica se houveram alterações no banco de dados ou nos binários compilados
          $changes = git status --porcelain
          if ($changes) {
            Write-Host "Novas atualizações regulatórias ou binários compilados encontrados! Enviando..."
            git add src/data/database.ts bin/server.exe bin/crawler.exe
            git commit -m "auto: atualização de normas (Rust) e binários compilados [scraped]"
            git push
          } else {
            Write-Host "Nenhuma alteração detectada nas legislações ou binários hoje."
          }

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Node Dependencies
        run: npm install

      - name: Build Next.js Static Site
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: out
          branch: gh-pages
          clean: true
